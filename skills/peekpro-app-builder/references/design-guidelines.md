# Peek Pro design & style guidelines (PLACEHOLDER — TBD)

Apps that render UI inside or alongside Peek Pro should follow Peek's **standard designs and
style conventions** so they feel native to the platform. The concrete assets for this are
**not finalized yet.**

> Status: **TBD.** The design system, stylesheets, component library, and brand guidance are
> still being defined by Peek. Until then, do not invent Peek-branded styles — keep UI clean,
> accessible, and neutral, and tell the user this guidance is pending.

## What will live here (once available)

- **`ASK THE MCP`** for the current/"latest" design-system version, if Peek chooses to serve
  design tokens or component metadata live (the hybrid layer) so this doesn't go stale.
- `TODO(verify)`: do developers get a **Peek UI component package** (like the Node SDK), or
  **CSS/design tokens + written guidelines** they implement themselves? — undecided.
- `TODO(verify)`: brand assets (logo usage, color palette, typography, spacing).
- `TODO(verify)`: accessibility requirements and any required layout conventions for the
  client-facing surface vs. the admin surface.

## Interim guidance for the build

- Don't fabricate Peek branding, colors, or logos.
- Use the chosen stack's well-supported, accessible UI approach (research current best
  practices live), and keep components easy to restyle once Peek's design system lands.
- Flag to the user that styling will need a pass once the Peek design guidelines are
  published.
